# =============================================================================
# Accesly - Environment Variables
# Copy this file to .env and fill in your values
# =============================================================================

# --- Supabase ---
# Get these from: https://supabase.com/dashboard → Project Settings → API
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# --- Stellar / Soroban ---
# RPC endpoint for Soroban testnet
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
SOROBAN_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# WASM hash of the deployed account abstraction contract
# Get it from: stellar contract info --contract-id <ID> --network testnet
WASM_HASH=your_wasm_hash_hex

# Funded Stellar account that pays for contract deployment and transactions
# Generate with: stellar keys generate server-key --network testnet
# Fund with: stellar keys fund server-key --network testnet
STELLAR_SERVER_SECRET=S...your-server-secret-key

# --- SEP-10 (Stellar Web Authentication) ---
# Separate keypair for signing SEP-10 challenges (can be same as server)
SEP10_SERVER_SECRET=S...your-sep10-secret-key
SEP10_HOME_DOMAIN=localhost:4321

# --- Security ---
# 32-byte hex key for encrypting secret keys in the database
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_char_hex_encryption_key

# Secret for signing JWT tokens (SEP-10 auth tokens)
JWT_SECRET=your_jwt_secret_here

#AWS KMS for encrypting sensitive data (Stellar secret keys)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_KMS_KEY_ARN=

#Google KMS for encrypting sensitive data (Stellar secret keys)
GCP_KMS_PROJECT_ID=
GCP_KMS_LOCATION=
GCP_KMS_KEY_RING=
GCP_KMS_KEY_NAME=
GCP_SERVICE_ACCOUNT_JSON=